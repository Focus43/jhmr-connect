/* global FastClick */

;(function( window, angular, undefined ){ 'use strict';

    angular.module('jhmrc', [
        'jhmrc.common',
        'jhmrc.login',
        'jhmrc.participant'
    ]).

        config([
            function(){

            }
        ]).

        run([
            function(){

            }
        ]);

})( window, window.angular );